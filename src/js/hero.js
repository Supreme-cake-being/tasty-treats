const orderBtnEl = document.querySelector('.order-btn');
console.log(orderBtnEl);

orderBtnEl.addEventListener;

orderBtnEl.addEventListener('click', onOrderBtnClick);

function onOrderBtnClick(event) {}
